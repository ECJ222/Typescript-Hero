import { ReactComponent as Logo } from "../assets/icons/logo.svg";

const Header = () => {
  return (
    <header>
      <nav>
        <Logo />
      </nav>
    </header>
  );
};

export default Header;
